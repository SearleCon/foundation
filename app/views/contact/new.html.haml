= simple_form_for @message, :url => send_contact_email_path do |form|
  = form.input :name
  = form.input :email
  = form.input :subject
  = form.text_area :body
  = form.button :submit, class: 'btn btn-primary'
